# Contact form (/contact); mailto links use SiteConfig contact.email (not this var)
NEXT_PUBLIC_FORMSPREE_ENDPOINT=https://formspree.io/f/meoljlry

SITE_ID=beauty-station
NEXT_PUBLIC_SITE_ID=beauty-station

API_URL=http://localhost:5000
NEXT_PUBLIC_API_URL=http://localhost:5000

NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Cookie max-age (seconds) — align with server JWT_ACCESS_EXPIRES_IN / JWT_REFRESH_EXPIRES_IN
NEXT_PUBLIC_ACCESS_TOKEN_MAX_AGE=900
NEXT_PUBLIC_REFRESH_TOKEN_MAX_AGE=604800

# Google sign-in button (@react-oauth/google) — must match server GOOGLE_CLIENT_ID
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Path deploy only: NEXT_PUBLIC_BASE_URL=
